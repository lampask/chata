using UnityEngine;
using UnityEngine.UI;

public class GameInterfaceUnit : MonoBehaviour
{
    public enum UIType {
        TIMER,
        CRYSTALS,
    }

    void UI_Toggle(UIType ui) {
        //TODO toggles ui visibility
    }
}
